apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: domainturva-client
  namespace: argocd
spec:
  project: domainturva
  source:
    repoURL: https://github.com/atk-works/domainturva-core
    targetRevision: main
    path: kubernetes/client
  destination:
    server: https://kubernetes.default.svc
    namespace: domainturva
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - Validate=false
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
